{
  "//": "Copy-paste detector — a HARD GATE in `npm run check` (and the pre-commit hook). `threshold` is the max % of duplicated lines allowed; the build fails above it. RATCHET IT DOWN AGGRESSIVELY — keep it a hair above the current floor, never leave slack. The slack is what lets a clone slip in unnoticed. Set threshold ≈ floor + 0.04 so ANY new ~8-line clone trips it; when you pay a clone off, drop the threshold to the new floor in the SAME commit. STARTING FLOOR IS HIGH ON PURPOSE (6.28%) — this gate was added long after the code, so it starts by locking in 'no worse' rather than pretending the code is clean. It is not a target, it is a ceiling that only ever comes down. Measured at introduction: routes 8.14% (65 clones) · services 4.86% (7) · middleware 2.33% (1) · jobs 1.57% (1) · lib and constants 0.00%. NEXT, in rough order of value-per-risk: (1) services/openai.js — SIX near-identical fetch + 429-backoff blocks across identifyElements/suggestCraftGuide/validateCakeImage/analyzeCake/embedText/suggestDescription/suggestBuildGuide; a single postJson helper collapses all of them and it is pure plumbing, but it is also the code that spends money and has no test coverage, so it wants its own reviewed commit. (2) routes/* — EXAMINED 2026-07-31 AND DELIBERATELY NOT REFACTORING. Most of what jscpd matches there is Express SHAPE, not logic: `router.post(path, requireAuth, requireCapability(cap), …)` signatures, `res.json(data)`, and the `} catch (err) { serverError(req, res, err) }` tail that every handler must repeat. The reported 8.14% also overstates it — jscpd skips comment TOKENS but reports LINE SPANS, so in a file commented as heavily as these a 25-line 'clone' can be six lines of boilerplate with a comment block sitting between them. The one genuinely extractable thing is the master-data CRUD shared by materials/textures/textStyles/cakeShapes/nozzles/tags — and a factory for it would turn the CAPABILITY GATE into a parameter, which is an authorization change in a repo with no test coverage. Not worth it to move a number. If it is ever attempted, build a route→method→capability inventory gate FIRST and require a zero diff across the refactor. A deliberate, reviewed clone goes between `// jscpd:ignore-start` … `// jscpd:ignore-end` so it doesn't count. History: 6.28% (gate introduced, no refactoring).",
  "threshold": 6.32,
  "minTokens": 50,
  "minLines": 8,
  "absolute": true,
  "gitignore": true,
  "ignore": ["**/*.test.js", "**/node_modules/**", "**/migrations/**", "**/supabase/**"],
  "reporters": ["console"]
}
